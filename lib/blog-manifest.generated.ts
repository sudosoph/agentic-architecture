// AUTO-GENERATED — do not edit manually; regenerated on every build via scripts/generate-blog-manifest.mjs
import type { PostFrontmatter } from './mdx'

export const blogPosts: Array<{ slug: string; frontmatter: PostFrontmatter; html: string }> = [
  {
    "slug": "96gb-ram-thesis",
    "frontmatter": {
      "title": "The 96GB RAM Thesis",
      "slug": "96gb-ram-thesis",
      "publishedDate": "2026-04-28",
      "description": "Achieving 28.4 t/s on AMD Strix Point via ROCm 7.3 — and what it means for local agentic workflows.",
      "tags": [
        "benchmarks",
        "amd",
        "rocm",
        "local-inference"
      ],
      "draft": false
    },
    "html": "<p><strong>TL;DR.</strong> A Framework 16 with a Ryzen AI 9 HX 370, Radeon RX 7700S, and 96GB of DDR5 runs GLM-4 9B at Q8_0 at <strong>28.4 tokens/sec</strong> under ROCm 7.3, with a 90GB GART carve-out feeding the iGPU directly out of system RAM. That number is not a benchmark trophy. It is the threshold at which agentic loops stop being a budgeting problem and start being an architecture problem — which is a much better problem to have.</p>\n<p>This post is the case for why 96GB of unified memory, not 64 and not 128, is the inflection point for solo builders running serious agentic workloads in 2026. It covers the hardware, the ROCm setup, the actual benchmark methodology, and the cost math against the frontier APIs.</p>\n<h2>The thesis in one paragraph</h2>\n<p>If you are running multi-agent loops — planner → researcher → critic → executor, looped with retries — you cannot do it on metered cloud APIs at scale unless you have already raised money. A reasonably busy agentic dev loop today burns 5–20M tokens a day. At Claude Sonnet 4 prices, that's $15–$60. At GPT-5 reasoning prices, multiply that. Per developer, per day. Multiply by a team. Multiply by the experiments you don't run because you flinched at the bill.</p>\n<p>96GB of system RAM, with most of it pinned as GART for an iGPU/dGPU pair that ROCm 7.3 finally treats as first-class, removes the budgeting layer. You stop asking <em>\"can I afford to let this agent retry?\"</em> and start asking <em>\"is the loop converging?\"</em> — which is the only question that actually matters.</p>\n<h2>The hardware</h2>\n<p>This is what's under the keyboard right now:</p>\n<pre><code>Mainboard:  Framework 16 (AMD)\nSoC:        AMD Ryzen AI 9 HX 370 (Strix Point, Zen 5 + RDNA 3.5 + XDNA 2)\ndGPU:       Radeon RX 7700S (Navi 33, 8GB GDDR6)\nMemory:     96GB DDR5-5600 (2 × 48GB SO-DIMM)\nStorage:    2TB NVMe (Gen 4)\nOS:         Ubuntu 24.04, kernel 6.11\n</code></pre>\n<p>A few notes on each line, because all of them are load-bearing:</p>\n<p><strong>Strix Point's iGPU (Radeon 890M, RDNA 3.5)</strong> is the actual workhorse for sustained inference under 12B params. It's tied to system RAM, which is the whole point. The dGPU is for bursty workloads — the cold start of a 13B model, image generation, occasional finetuning runs.</p>\n<p><strong>96GB, not 64, not 128.</strong> 64GB hits a wall the moment you want to keep a 9B model resident, run a context window over 32k, <em>and</em> leave headroom for VS Code + Chrome + n8n + a vector DB. 128GB exists as a Framework 16 option, but only as 2 × 64GB DDR5-5200 — slower, and the marginal $400 buys you tokens you will never spend on inference. You get more value buying a second NVMe and offloading swap there.</p>\n<p><strong>Ubuntu 26.04 LTS (Resolute Raccoon).</strong> GNOME 50 on Wayland-only, memory-safe Rust coreutils, systemd 259 with mandatory cgroup v2, TPM-backed disk encryption out of the box. ROCm 7.3 builds cleanly. The 26.04 kernel ships AMDGPU support for Strix Point in the GA tree — no HWE stack juggling, no DKMS gymnastics. If you are still on 24.04, this release is worth the upgrade for the AMD path alone.</p>\n<h2>The 90GB GART trick</h2>\n<p>GART (Graphics Aperture Remapping Table) is the mechanism by which the iGPU sees system RAM as if it were VRAM. By default, Ubuntu hands the iGPU something miserable like 4GB of GART. To run a 9B model at Q8_0 (~9.5GB on disk, more in memory with the KV cache) you need GART to be much larger.</p>\n<p>The setting is in <code>/etc/default/grub</code> as a kernel parameter:</p>\n<pre><code>GRUB_CMDLINE_LINUX_DEFAULT=\"quiet splash amdgpu.gttsize=92160\"\n</code></pre>\n<p><code>gttsize</code> is in MB. 92160MB = 90GB. Then:</p>\n<pre><code class=\"language-bash\">sudo update-grub &#x26;&#x26; sudo reboot\n</code></pre>\n<p>After the reboot, <code>glxinfo -B</code> should report the new GTT size, and <code>rocm-smi --showmeminfo vram</code> will show the iGPU now has the headroom you actually paid for. This is the single config change that does the most work in this entire stack.</p>\n<p>Leave 6GB of system RAM unmapped from GART — that's your floor for the OS, your editor, and the chrome tabs you are about to ignore for the next three hours.</p>\n<h2>The ROCm 7.3 install (the part that used to be painful)</h2>\n<p>ROCm 7.3 is the first release where I would call Strix Point + RDNA 3.5 a supported configuration instead of a science project. Earlier 7.x releases shipped HIP runtimes that crashed on <code>gfx1150</code> (the iGPU's ISA code) under any meaningful load. The 7.3 packages handle it. Install via the official APT repo:</p>\n<pre><code class=\"language-bash\">sudo apt update\nsudo apt install python3-setuptools python3-wheel\nwget https://repo.radeon.com/amdgpu-install/7.3/ubuntu/noble/amdgpu-install_7.3.x_all.deb\nsudo apt install ./amdgpu-install_7.3.x_all.deb\nsudo amdgpu-install -y --usecase=rocm\nsudo usermod -aG render,video $USER\n</code></pre>\n<p>Reboot. Then verify:</p>\n<pre><code class=\"language-bash\">rocm-smi\nrocminfo | grep gfx\n# expected: gfx1150 (iGPU), gfx1102 (RX 7700S)\n</code></pre>\n<p>If <code>rocminfo</code> lists both <code>gfx1150</code> and <code>gfx1102</code>, you have a working pair. The HIP runtime can target either, and llama.cpp will pick whichever you point <code>HIP_VISIBLE_DEVICES</code> at.</p>\n<h2>Building llama.cpp for HIP</h2>\n<p>llama.cpp's HIP backend has been stable on RDNA 3 for a year and on RDNA 3.5 since around February 2026. Build it directly — the prebuilt binaries from the releases page miss the <code>gfx1150</code> target.</p>\n<pre><code class=\"language-bash\">git clone https://github.com/ggml-org/llama.cpp\ncd llama.cpp\ncmake -B build -DGGML_HIP=ON \\\n  -DAMDGPU_TARGETS=\"gfx1150;gfx1102\" \\\n  -DCMAKE_BUILD_TYPE=Release\ncmake --build build --config Release -j\n</code></pre>\n<p>The <code>AMDGPU_TARGETS</code> list is the part that matters. You're telling the HIP compiler to emit code for both the iGPU and the dGPU in the same binary so you can switch between them with an environment variable. Build time is around 6 minutes on this machine. The resulting <code>build/bin/llama-server</code> is the binary you actually care about.</p>\n<h2>The benchmark</h2>\n<p>The model is GLM-4 9B Q8_0 — Zhipu AI's 9B-parameter Chinese-and-English instruction-tuned model, quantized to 8-bit. It was, at the time I ran this benchmark, the best open model under 10B for tool-calling and structured output. The 2026 frontier has since shifted to Qwen 3.6 (the 27B Dense variant beats Qwen's own 397B MoE on agentic coding) and Gemma 4 (E4B for fast routing, 26B MoE for high-throughput agents). I run those now for daily work; GLM-4 stays in the rotation as the headline benchmark because it is the apples-to-apples comparison everyone else has run too.</p>\n<pre><code class=\"language-bash\"># Pin to the iGPU so the benchmark is honest about what Strix Point can do\nHIP_VISIBLE_DEVICES=0 \\\n./build/bin/llama-bench \\\n  -m models/glm-4-9b-chat.Q8_0.gguf \\\n  -p 512 -n 256 -t 8 \\\n  -ngl 99\n</code></pre>\n<p>The relevant line from the output:</p>\n<pre><code>| model         | size   | params | backend | ngl | test  |          t/s |\n| GLM-4 9B Q8_0 | 9.5 GB | 9.4 B  |   ROCm  |  99 |  tg256| 28.42 ± 0.31 |\n</code></pre>\n<p><strong>28.4 tokens/sec, sustained, on the iGPU alone.</strong> Prompt processing (<code>pp512</code>) is faster — around 340 t/s — which matters because most agentic turns are heavy on prompt and light on generation.</p>\n<p>For comparison, the same model at Q8_0 on:</p>\n<table>\n<thead>\n<tr>\n<th>Hardware</th>\n<th>tg t/s</th>\n<th>Cost / month</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Strix Point iGPU (this rig)</td>\n<td>28.4</td>\n<td>$0 (owned)</td>\n</tr>\n<tr>\n<td>RX 7700S dGPU (8GB)</td>\n<td>~35</td>\n<td>(offload only)</td>\n</tr>\n<tr>\n<td>RTX 4070 (12GB)</td>\n<td>~52</td>\n<td>$600+ card</td>\n</tr>\n<tr>\n<td>Mac Studio M3 Ultra (96GB)</td>\n<td>~42</td>\n<td>$4,800</td>\n</tr>\n<tr>\n<td>Cloud H100 (per hour)</td>\n<td>180+</td>\n<td>$2.49/hr</td>\n</tr>\n</tbody>\n</table>\n<p>The H100 is faster per second. It is not faster per dollar, and it is not faster per <em>retry attempt at 3 a.m. when you've already burned your daily budget</em>.</p>\n<h2>The economic case</h2>\n<p>Here is the argument I keep making to founders: if your agentic workflow needs to run <em>unattended for hours</em>, the cost of cloud inference is not your AWS bill — it is your willingness to let the agent retry. Every retry is a tokens decision, and humans ration tokens like they ration calories: badly, and with consequences for the work.</p>\n<p>Concrete math, based on a real agentic dev loop I ran last week:</p>\n<ul>\n<li><strong>Loop</strong>: planner → 3 parallel research agents → critic → executor → verifier</li>\n<li><strong>Avg tokens per full loop</strong>: 180,000 in, 12,000 out</li>\n<li><strong>Loops per hour during active development</strong>: 8–12</li>\n<li><strong>Sustained dev session</strong>: 4 hours</li>\n</ul>\n<p>That's roughly <strong>6M input tokens and 400k output tokens per session</strong>. On Claude Sonnet 4: ~$22/session. On GPT-5 reasoning: more. Per developer, per day. The cost is real, but the <em>behavioral</em> cost is what kills the loop — you start trimming retries, you start running fewer parallel agents, you let the model \"good enough\" instead of letting the critic loop converge.</p>\n<p>On this machine, that same session is electricity. ~65W under sustained load × 4 hours = 0.26 kWh × $0.13 = <strong>$0.034</strong>. Three cents. The agent runs all night because there is no reason it shouldn't.</p>\n<h2>What this actually unlocks</h2>\n<p>The interesting consequence is not \"I saved money on tokens.\" It is the <em>regime change</em> in how you design loops:</p>\n<ol>\n<li><strong>Parallel agent fan-out becomes free.</strong> Five $0.01 cloud agents and one $0.05 monolith are roughly the same. Five local agents and one local monolith are the same too — except the local fan-out finishes in parallel and the cloud one finishes when the rate limiter says so.</li>\n<li><strong>The critic gets to be expensive.</strong> When generation is free, you can spend three turns of the critic arguing with the executor before you commit. This is the single largest quality unlock in agentic design and it is paywalled by token cost on metered APIs.</li>\n<li><strong>You can leave loops running.</strong> A research agent that wakes up every 15 minutes to scrape, summarize, and update a vector store is a $300/month subscription on cloud APIs. Locally it is the same as leaving Slack open.</li>\n<li><strong>Experiments stop being expensive.</strong> \"What if the planner ran twice and we picked the better plan?\" becomes a one-line change instead of a budget meeting.</li>\n</ol>\n<h2>The catch (be honest about this)</h2>\n<p>96GB on Strix Point does not give you GPT-5. You will not run a 200B-parameter MoE on this hardware. What you get is:</p>\n<ul>\n<li>7B–14B models at Q5–Q8, comfortably, with full context (32k–128k depending on model).</li>\n<li>A 27B model (Gemma 3 27B, Qwen 3 32B) at Q4_K_M with effort.</li>\n<li>No frontier-class single-shot reasoning. The frontier models still earn their bill on the <em>hardest</em> problems.</li>\n</ul>\n<p>The right pattern is hybrid: local for the 95% of agentic turns that are routing, summarizing, drafting, calling tools, and critiquing; frontier API for the 5% of turns where you genuinely need the smartest model in the world. A well-designed loop calls the frontier ~once per session, not 80 times.</p>\n<h2>What I'm watching next</h2>\n<p>Three things on the radar:</p>\n<ul>\n<li><strong>Strix Halo (Ryzen AI Max+ 395)</strong>: 256GB unified memory option, 32 RDNA 3.5 CUs, available in mini-PC form factors now. Doubles the model ceiling. The RAM is faster too. If you don't need a laptop, this is the obvious next move.</li>\n<li><strong>ROCm 8.0</strong>: rumored to land Q3 2026 with first-class XDNA 2 NPU offload for the matmul-heavy parts of attention. If that ships, the iGPU stops being the bottleneck on long-context turns.</li>\n<li><strong>GLM-5 and Qwen 3.5</strong>: both rumored to release 12B-class models tuned specifically for agentic tool use. The thesis only gets stronger as the open models catch up to closed ones at sub-12B sizes.</li>\n</ul>\n<h2>The takeaway</h2>\n<p>The 96GB number is not the point. The point is that the line between \"local inference is a hobby\" and \"local inference is the production substrate for my agentic stack\" gets crossed somewhere around 96GB of fast unified memory paired with ROCm 7.3-class drivers. That line was crossed for me in April 2026. If you are still architecting your agents around token budgets in 2026, you are paying a tax that the giants are charging because they can.</p>\n<p>The full configuration — kernel params, ROCm version, model files, llama.cpp HIP flags, n8n workflows that drive this stack — will land in <a href=\"https://github.com/sudosoph\"><code>apu-config</code></a> shortly. If you want a heads-up when the OSS tooling drops, the newsletter is the place. If you want the same setup running in your office by next week, <a href=\"/work-with-me\">that is what an engagement looks like</a>.</p>"
  },
  {
    "slug": "mcp-101",
    "frontmatter": {
      "title": "MCP 101: From Function Call to Driver Layer",
      "slug": "mcp-101",
      "publishedDate": "2026-05-06",
      "description": "What the Model Context Protocol actually is, what it gets right, where it leaks, and why the local-first version is the cleaner story.",
      "tags": [
        "mcp",
        "agents",
        "protocols",
        "local-first",
        "ollama"
      ],
      "draft": false
    },
    "html": "<p><strong>TL;DR.</strong> The Model Context Protocol is the closest thing the agentic stack has to a POSIX-style boundary between the model and the world. It is JSON-RPC 2.0 over stdio or streamable HTTP, with a handshake that negotiates capabilities, a runtime tool catalog the host can subscribe to, and a recursive <em>sampling</em> primitive that lets a server call back into the host's model. That last detail is what makes it more than function calling — it's the protocol that lets tools and models be <em>coroutines</em>, which is the actual operating-system abstraction underneath. This post is the honest version of MCP in May 2026: what it is, what works, where the real footguns live, and what the local-first reading looks like for engineers running their own models.</p>\n<h2>What it actually is, in 60 seconds</h2>\n<p>Model Context Protocol is a protocol for connecting LLM applications to the world outside the LLM. The latest revision is dated <code>2025-11-25</code> (the spec uses the date as the version string, which is a small but lovely choice). It defines:</p>\n<ul>\n<li><strong>A wire format</strong>: JSON-RPC 2.0 messages over a transport.</li>\n<li><strong>Two transports</strong>: <code>stdio</code> (subprocess, stdin/stdout) and <strong>streamable HTTP</strong> (the network-friendly option that replaced the original SSE-only transport in early 2025).</li>\n<li><strong>Three roles</strong>: a <em>host</em> (the LLM application — Claude Desktop, Cursor, Zed, Claude Code), a <em>client</em> (one inside the host per server), and a <em>server</em> (the thing exposing capabilities).</li>\n<li><strong>Three server primitives</strong>: <code>tools</code> (model-controlled functions), <code>resources</code> (readable context — files, DB rows, URIs, with optional subscription), <code>prompts</code> (user-controlled templates that show up as slash commands).</li>\n<li><strong>Three client primitives</strong>: <code>sampling</code> (the server asks the host's LLM to do an inference — recursive), <code>roots</code> (the server asks the client what filesystem boundaries it can operate in), <code>elicitation</code> (the server asks the user a question mid-execution).</li>\n</ul>\n<p>The official spec compares MCP to the <strong>Language Server Protocol</strong>, and that's the right reference. Before LSP, every editor implemented every language: N×M problem. After LSP, N+M. Every editor reads the protocol; every language ships one server. That's exactly the deal MCP is offering for AI hosts and tool sources. Forget USB-C and forget \"function calling but better\" — those are marketing analogies. LSP is the engineering one.</p>\n<p>But the deeper analogy I keep coming back to is <em>device drivers</em>. Each MCP server is a driver that exposes a uniform interface for some piece of the world (your filesystem, your git repo, your Postgres database, a web browser). The host is a kernel arbitrating between drivers and processes. The LLM is the userspace process making syscalls. The <code>roots</code> primitive negotiates filesystem boundaries — that is, mount points. The <code>tasks</code> capability (added in <code>2025-11-25</code>) is process management. The recursive <code>sampling</code> primitive is the syscall going <em>back into</em> the kernel from the driver. This is genuinely OS-shaped, and it's why I think the \"MCP is just an API\" critique misses the point.</p>\n<h2>The handshake, copy-pasteable</h2>\n<p>This is the part most posts skip. Here's the actual exchange when an MCP client connects to a server:</p>\n<pre><code class=\"language-json\">// 1. Client → server: initialize\n{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"initialize\",\n \"params\":{\"protocolVersion\":\"2025-11-25\",\n  \"capabilities\":{\n    \"roots\":{\"listChanged\":true},\n    \"sampling\":{},\n    \"elicitation\":{}},\n  \"clientInfo\":{\"name\":\"my-host\",\"version\":\"1.0.0\"}}}\n\n// 2. Server → client: capabilities mirror\n{\"jsonrpc\":\"2.0\",\"id\":1,\"result\":{\n  \"protocolVersion\":\"2025-11-25\",\n  \"capabilities\":{\n    \"tools\":{\"listChanged\":true},\n    \"resources\":{\"subscribe\":true,\"listChanged\":true},\n    \"prompts\":{\"listChanged\":true},\n    \"logging\":{}},\n  \"serverInfo\":{\"name\":\"my-server\",\"version\":\"1.0.0\"}}}\n\n// 3. Client → server: ack\n{\"jsonrpc\":\"2.0\",\"method\":\"notifications/initialized\"}\n</code></pre>\n<p>The two things to notice: capabilities are negotiated bilaterally (each side declares what it supports), and <code>listChanged</code> notifications mean the catalog is <em>live</em> — the server can push updates and the host must invalidate its cached tool list. A function-calling API can't do this.</p>\n<p>The actual config most users see is the <code>mcpServers</code> block in their host's settings file. A real one from a working setup:</p>\n<pre><code class=\"language-json\">{\n  \"mcpServers\": {\n    \"filesystem\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"@modelcontextprotocol/server-filesystem\", \"/Users/me/code\"]\n    },\n    \"git\": {\n      \"command\": \"uvx\",\n      \"args\": [\"mcp-server-git\", \"--repository\", \"/Users/me/code/site\"]\n    },\n    \"playwright\": {\n      \"command\": \"npx\",\n      \"args\": [\"@playwright/mcp@latest\"]\n    },\n    \"github\": {\n      \"type\": \"streamable_http\",\n      \"url\": \"https://api.githubcopilot.com/mcp/\",\n      \"headers\": { \"Authorization\": \"Bearer ${GITHUB_PAT}\" }\n    }\n  }\n}\n</code></pre>\n<p>Four stdio servers and one streamable-HTTP server in the same file. That mix-and-match is the whole pitch.</p>\n<h2>The ecosystem state, May 2026</h2>\n<p>A snapshot of what's actually in production use:</p>\n<ul>\n<li>\n<p><strong>Reference servers</strong> — the canonical Anthropic-published list at <code>github.com/modelcontextprotocol/servers</code> is now down to seven: <code>everything</code>, <code>fetch</code>, <code>filesystem</code>, <code>git</code>, <code>memory</code>, <code>sequential-thinking</code>, <code>time</code>. Notably <em>moved out</em> (now in the archived sibling repo): GitHub, GitLab, Slack, Postgres, SQLite, Sentry, Redis, Puppeteer, Google Drive, Google Maps, Brave Search, AWS KB Retrieval. They were superseded by <em>first-party</em> servers from those vendors. That's a sign of maturity, not deprecation.</p>\n</li>\n<li>\n<p><strong>First-party heavyweights</strong> — the GitHub MCP server (~30k stars, 80+ tools, hosted at <code>api.githubcopilot.com/mcp/</code>) and Microsoft's Playwright MCP (~32k stars, accessibility-tree-driven browser automation, no screenshots) are the two biggest. The Cloudflare Workers/KV/R2 servers ship with OAuth pre-wired.</p>\n</li>\n<li>\n<p><strong>Bundling format</strong> — <code>.mcpb</code> (formerly <code>.dxt</code>) is a zip with a manifest and a bundled server. Spiritually similar to a <code>.vsix</code> for VS Code or a <code>.crx</code> for Chrome. Single-click install for end users, distribution-ready.</p>\n</li>\n<li>\n<p><strong>SDK story</strong> — official SDKs in TypeScript, Python (FastMCP is the canonical wrapper), Go, Rust, Java, Kotlin, C#, Swift, Ruby. The Python <em>hello world</em> with FastMCP is genuinely about ten lines:</p>\n<pre><code class=\"language-python\">from mcp.server.fastmcp import FastMCP\nmcp = FastMCP(\"HelloWorld\")\n\n@mcp.tool()\ndef hello(name: str = \"World\") -> str:\n    \"\"\"Greet someone by name.\"\"\"\n    return f\"Hello, {name}!\"\n\nif __name__ == \"__main__\":\n    mcp.run(transport=\"stdio\")\n</code></pre>\n</li>\n<li>\n<p><strong>Client matrix</strong> — Claude Desktop, Claude Code, Cursor, Cline (VS Code), Continue.dev, Zed, VS Code GitHub Copilot. All first-class. Ollama is the conspicuous exception — see below.</p>\n</li>\n</ul>\n<h2>The 4-legged identity problem (this is the part nobody warns you about)</h2>\n<p>Matthew Xu gave the sharpest talk on this at the AI Dev conference in San Francisco last week, and I haven't seen a clearer framing anywhere else. The short version:</p>\n<p>OAuth was designed for a triangle. User → app → identity provider. The app gets a token that represents the user, makes API calls, and APIs validate the token against scopes. Three legs, three actors, deterministic.</p>\n<p>Add an agent and an MCP server to that triangle and you get a square. <strong>User → agent → MCP → API.</strong> Suddenly the API has no idea who triggered the request. The MCP server is calling on behalf of an agent calling on behalf of a user — but the auth header at the API only sees the MCP server's credentials. Identity <em>gets lost mid-chain</em>, and the workarounds are all bad:</p>\n<ul>\n<li><strong>Non-delegating client credentials</strong>: the agent scraps user intent and the MCP server uses its own credentials. Works, but the API has no audit trail of who actually triggered the call.</li>\n<li><strong>Pre-injected tokens</strong>: the agent fetches every downstream token upfront. Requires the agent to know all downstream APIs in advance, which is impossible for agents that decide tools at runtime.</li>\n<li><strong>Out-of-band login</strong>: the user authorizes every tool at every step. UX disaster.</li>\n<li><strong>Token exchange (RFC 8693)</strong>: the actual right answer — the agent exchanges its user token for an audience-scoped token before each downstream call. Limited IdP support: Keycloak does it, Microsoft doesn't, Okta only via claims.</li>\n</ul>\n<p>The MCP spec, to its credit, codified the four RFCs that make this work end-to-end:</p>\n<ul>\n<li><strong>RFC 9728</strong> for resource metadata (the agent figures out which auth server to talk to),</li>\n<li><strong>RFC 8414</strong> for authorization-server metadata (endpoints, capabilities, sign-in keys),</li>\n<li><strong>RFC 7591</strong> for dynamic client registration (the agent registers itself programmatically),</li>\n<li><strong>RFC 8693</strong> for token exchange (audience-scoped tokens at every hop).</li>\n</ul>\n<p>In production, you stand up a <em>broker layer</em> that handles this entire dance, caches tokens, integrates with secret-management systems, and writes an immutable audit log of every hop in the chain. That's a week of infrastructure work. It is genuinely the right answer for cloud-agent systems and it is genuinely a lot of work.</p>\n<h2>The local-first angle (this is the part most posts miss entirely)</h2>\n<p>Here's the thing about the 4-legged identity problem: <strong>on a single-machine local setup, the equivalent of all of that is filesystem permissions and environment variables.</strong> The MCP spec itself carves a quiet exception in for stdio transport:</p>\n<blockquote>\n<p>\"Implementations using a STDIO transport SHOULD NOT follow this specification, and instead retrieve credentials from the environment.\"</p>\n</blockquote>\n<p>That's the spec authors saying <em>if it's all on your machine, don't bother with OAuth</em>. Use the environment. Use file permissions. The protocol gets out of your way. This is the part of MCP I find most interesting and the angle I haven't seen written up: the protocol's value is independent of whether the model is yours. <strong>The harness is sovereign even when the model isn't.</strong> And when the model is also yours, the auth story collapses to almost nothing.</p>\n<p>Now the awkward part. <strong>Ollama does not natively speak MCP.</strong> Ollama implements its own tool-calling API, OpenAI-shaped, returning <code>tool_calls</code> on <code>/api/chat</code>. To use MCP servers with Ollama you need a bridge client — the community standard right now is <code>jonigl/mcp-client-for-ollama</code>. The architecture:</p>\n<ol>\n<li>The bridge reads a standard <code>mcpServers</code> config (same shape as Claude Desktop's).</li>\n<li>It calls <code>tools/list</code> on each MCP server, flattens the catalog into Ollama's <code>tools</code> array.</li>\n<li>User query → Ollama (with tools) → if <code>tool_calls</code> → bridge calls the appropriate MCP server's <code>tools/call</code> → results piped back → Ollama synthesizes.</li>\n<li>Supports stdio, SSE, and streamable HTTP transports.</li>\n</ol>\n<p>This works, and I run it daily. But there is a real gap here that nobody has filled: <strong>the host application is doing work that should arguably be in Ollama itself.</strong> As of May 2026 nobody has merged native MCP into Ollama upstream, and llama.cpp doesn't speak it directly either. There's an opportunity-shaped hole in the local-first stack. If you're a Go engineer reading this, that PR is worth more than most startup ideas.</p>\n<p>The corollary: when you can run MCP locally, the threat model collapses. Tushar Jain from Docker showed at AI Dev a per-agent MicroVM sandbox that limits what an agent can touch in a cloud setup. The local-native version is even simpler: don't give the agent network access in the first place. A local model running in a network-restricted container with only stdio MCP servers can write all the code it wants and exfiltrate exactly nothing. The lethal-trifecta attack — private data + untrusted content + exfiltration — only works if all three legs exist. Cut the network leg and the attack surface collapses.</p>\n<h2>The honest critiques</h2>\n<p>I am bullish on MCP and I think you should be too, but the discourse is full of valid complaints and pretending otherwise costs credibility. The list:</p>\n<ul>\n<li><strong>The security model is opt-in.</strong> Most of the security guidance in the spec uses <code>SHOULD</code> instead of <code>MUST</code>. Treat the SHOULDs as MUSTs. Hosts that implement the bare-minimum version of the spec are buyable, and \"buyable\" here means \"exploitable.\"</li>\n<li><strong>Tool poisoning is a real attack class.</strong> A malicious server can ship tool descriptions with hidden instructions the model reads as commands. The April 2025 Invariant Labs paper on this should be required reading. The mitigation is \"trust your servers,\" which is fine but not what most users do.</li>\n<li><strong>Confused-deputy and token-passthrough attacks</strong> were the most-exploited footgun in 2025. The spec explicitly forbids passthrough now: \"MCP servers MUST NOT accept any tokens that were not explicitly issued for the MCP server.\" Easy to violate accidentally.</li>\n<li><strong>One-click install is arbitrary code execution.</strong> The spec spells this out. A malicious startup command in a manifest is unrestricted shell access. Cursor and Claude Desktop do show the command, but most users click through. Sandboxing belongs at the host layer, and not all hosts implement it well.</li>\n<li><strong>The MCP plateau.</strong> Brandon Waselnuk from Unblocked called this out at AI Dev: more servers, same output quality. The agent doesn't know when to call which tool. He calls it the <em>satisfaction-of-search</em> problem — the agent finds the first matching answer and stops, even if it's wrong. The fix is curated context, not more servers. Connecting your agent to ten more MCP servers does not make it ten times smarter. Often it makes it slower.</li>\n<li><strong>The token cost of MCP itself.</strong> GitHub's official server exposes 80+ tools. Every turn of the conversation, the host has to remind the model that those tools exist, which costs tokens. There's a <code>--toolsets</code> flag for scoping precisely because this is a known footgun. Some teams have reverted to using the <code>gh</code> CLI directly because the agent already knows how to use it from training data and it costs roughly zero context.</li>\n<li><strong>No standard tracing.</strong> There's <code>logging</code> and progress notifications, but no spec-level OpenTelemetry support. Multi-hop topologies (host → MCP proxy → MCP server → upstream API) are hellish to debug. This will get fixed; it hasn't been yet.</li>\n<li><strong>Auth complexity is real.</strong> Doing OAuth correctly at every hop with PKCE + RFC 8707 + RFC 9728 + (DCR or CIMD) + RFC 8693 is not a weekend project. If you're standing up a remote MCP server with auth, budget a week.</li>\n</ul>\n<h2>What I'd build with it today</h2>\n<p>If you're at the <em>what should I actually try</em> stage, here is the order I'd recommend:</p>\n<ol>\n<li><strong>Local stdio servers, no network.</strong> Filesystem, git, memory. Configure them in Claude Code or Cursor. Watch the agent navigate your repo with intent. This is the \"oh, that's what they mean\" moment.</li>\n<li><strong>A custom server in FastMCP.</strong> Pick one tedious internal task (export a CSV from your CRM, run a deploy script, look up a customer's billing status) and write a 30-line MCP server for it. The act of <em>naming the tool</em> is the unlock — once the agent has it, it'll use it correctly far more often than you expect.</li>\n<li><strong>The Ollama bridge.</strong> Install <code>jonigl/mcp-client-for-ollama</code>, point it at the same <code>mcpServers</code> config, and run the same workflow against a local Qwen 3.6 27B or Gemma 4 26B MoE. You'll lose some intelligence relative to Sonnet 4. You'll also cut your daily token spend to zero. For the 80% of tasks that are routing, classifying, and tool-calling, the local model is fine.</li>\n<li><strong>Sandboxing.</strong> Once you trust the tools, tighten the noose. <code>docker/cagent</code> for containerized execution. Network egress disabled by default. Allowlist what you actually need. The blast radius shrinks dramatically.</li>\n<li><strong>A second client.</strong> Run the same servers from two different clients. Cursor and Claude Code, or Claude Desktop and Zed. The protocol guarantees portability; treat that guarantee like a property test. Anything that breaks across clients is something you depended on that you shouldn't have.</li>\n</ol>\n<h2>What I'm watching</h2>\n<p>Three threads:</p>\n<ul>\n<li><strong>Native MCP in Ollama or llama.cpp.</strong> Whoever lands this PR first removes the bridge-client tax for local users. There is genuine demand and no good upstream answer yet.</li>\n<li><strong>MCP for agent-to-agent communication.</strong> Vlad Luzin from Thenvoi gave a sharp talk at AI Dev arguing that MCP isn't actually a protocol for agents talking to each other — it's a protocol for an agent talking to its tools. Google's A2A is closer, but it has no registry yet and doesn't support multi-peer conversations. The shape of the answer here probably looks like \"MCP plus a directory service plus identity,\" and the team that ships it cleanly will own the agentic mesh.</li>\n<li><strong>Skills as the new package manager.</strong> Anthropic's skills launch in late 2025 was — in the words of one panel I sat through last week — <em>a bigger deal than MCP</em>. A skill is a folder with a <code>SKILL.md</code> and some scripts; the model loads it on demand for the cost of a few tokens. There's a real chance skills become the unit of code reuse in the agentic era and MCP is the <em>protocol</em> underneath them. Both stories can be true.</li>\n</ul>\n<h2>The takeaway</h2>\n<p>MCP is not magic. It's a wire format with a handshake and some good defaults. What makes it interesting is that it's the <em>first</em> wire format that treats the LLM as a first-class actor in a distributed system instead of a black box behind a single API call. The protocol gets the abstractions right: capabilities, recursion via sampling, runtime discovery, multi-transport. The implementation maturity is uneven — security is opt-in, tracing is missing, the auth story is heavy for what it does — but the shape is right.</p>\n<p>If you're a founder, you should be running it locally before you bother with the cloud version. If you're an engineer, you should write at least one server for something boring you do every week. If you're a security person, the lethal trifecta is the threat model that matters and you should already be auditing your host's sandboxing story.</p>\n<p>The full configs and the working Ollama bridge setup will land on <a href=\"https://github.com/sudosoph\">github.com/sudosoph</a> shortly. If you want a notification when the OSS tooling drops, the newsletter is the place. If you want it set up in your office by next week, <a href=\"/work-with-me\">that is what an engagement looks like</a>.</p>"
  },
  {
    "slug": "swarm-vs-monolith",
    "frontmatter": {
      "title": "Swarm vs Monolith",
      "slug": "swarm-vs-monolith",
      "publishedDate": "2026-05-08",
      "description": "Why five specialized $0.01 agents beat one $0.50 god model — and what the multi-agent crowd gets wrong about it.",
      "tags": [
        "agents",
        "architecture",
        "patterns",
        "production"
      ],
      "draft": false
    },
    "html": "<p><strong>TL;DR.</strong> The most reliable production agentic systems shipping in May 2026 do not look like one super-intelligent agent. They look like eight narrow specialists coordinated by a manager, each validated by a deterministic schema, each running on the cheapest model that can do its specific job. Bain &#x26; Company's HR Services payroll agent — a real production system processing one million customer emails per year at >98% accuracy — is <em>eight subgraphs in LangGraph</em>. It is the load-bearing example of the entire 2026 agentic architecture playbook, and the lesson is the one Sanjin Bicanic stated plainly at AI Agent Conference NYC last week: <strong>decomposition beats a super-agent</strong>. This post is the case for the swarm pattern, the case against the way most teams implement it, and the case that local-first inference is what makes it economically irresistible.</p>\n<h2>The number that ends the debate</h2>\n<p>Sanjin Bicanic and Xun Yang from Bain shared production data at AI Agent Conference NYC on May 4. The system, built for an HR services company that handles 1-in-10 private payrolls in the US:</p>\n<ul>\n<li><strong>Architecture:</strong> 8 LangGraph subgraphs, manager/worker pattern</li>\n<li><strong>Models:</strong> mixed (cheap routing, mid-tier reasoning, frontier only at the hard turn)</li>\n<li><strong>Validators:</strong> schema + plausibility rules running between every subgraph</li>\n<li><strong>Daily volume:</strong> 3,000+ live customer emails in shadow mode</li>\n<li><strong>Accuracy after 6 months:</strong> <strong>98%</strong>, matching the human specialist benchmark</li>\n<li><strong>Roadmap unlock:</strong> 100M+ emails per year addressable</li>\n</ul>\n<p>The closing slide of their talk is the line that should be on every engineer's wall:</p>\n<blockquote>\n<p>\"The agent engine is only 20% of the work. The other 80% is the durable system around it that will outlive the agent.\"</p>\n</blockquote>\n<p>The decomposed swarm is the <em>durable system</em>. The agent engine — whatever frontier model you happened to pick today — is the part that gets ripped out and replaced every six months as models improve. The architecture that survives is the one with narrow specialists, schemas, validators, and a control plane that lets you swap any one piece without rewriting the rest.</p>\n<h2>The cost math, again, because it matters</h2>\n<p>Pricing snapshot from May 2026:</p>\n<table>\n<thead>\n<tr>\n<th>Model</th>\n<th>Input $/M</th>\n<th>Output $/M</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Claude Opus 4.x</td>\n<td>$15</td>\n<td>$75</td>\n</tr>\n<tr>\n<td>Claude Sonnet 4.5</td>\n<td>$3</td>\n<td>$15</td>\n</tr>\n<tr>\n<td>Claude Haiku 4.5</td>\n<td>$1</td>\n<td>$5</td>\n</tr>\n<tr>\n<td>GPT-5 mini</td>\n<td>$0.25</td>\n<td>$1</td>\n</tr>\n<tr>\n<td>Gemini 2.5 Flash</td>\n<td>$0.30</td>\n<td>$2.50</td>\n</tr>\n<tr>\n<td>DeepSeek V4 Flash</td>\n<td>$0.14</td>\n<td>$0.28</td>\n</tr>\n</tbody>\n</table>\n<p>A \"god model\" turn — a single Sonnet 4.5 call doing planning + execution + verification on 50K tokens of context — runs roughly $0.35 in input + $0.15 in output. Call it <strong>$0.50 per turn</strong>.</p>\n<p>A swarm turn doing the same total work, decomposed:</p>\n<ul>\n<li>Router (Haiku 4.5, 5K tokens): $0.005 in + $0.0005 out → ~<strong>$0.006</strong></li>\n<li>Three parallel research workers (DeepSeek V4 Flash, 15K tokens each, fan-out): $0.006 in + $0.0008 out × 3 → ~<strong>$0.020</strong></li>\n<li>Critic (Haiku 4.5, 8K tokens): ~<strong>$0.009</strong></li>\n<li>Executor (Sonnet 4.5, 12K tokens): $0.036 in + $0.030 out → ~<strong>$0.066</strong></li>\n</ul>\n<p>Total swarm turn: <strong>~$0.10.</strong> <em>Five times cheaper than the monolith on the same task, with parallel research that completes in roughly one-third the wall-clock time.</em></p>\n<p>This is the math the hype cycle skipped past. Every doubling of fan-out with cheap specialists is a quarter the cost of vertical-scaling with the frontier model. <strong>Five $0.01 agents are not just cheaper than one $0.50 agent. They are also faster, more inspectable, and structurally easier to evaluate.</strong></p>\n<h2>Why the monolith fails in production</h2>\n<p>The pitch for the single-call god model is simplicity. One context, one model, one bill. In a demo, it looks great. The 65% solution Venky Veeraraghavan from DataRobot called out at AI Dev SF.</p>\n<p>What kills the monolith is the gap between 65% and 95%. Specifically:</p>\n<p><strong>Silent failures.</strong> A monolithic agent gives you one answer. If the answer is wrong, you have one trace to debug — usually 15K tokens of mixed reasoning, tool calls, and rationalization. Diagnosing <em>why</em> it produced that wrong answer is forensic archaeology. A decomposed swarm fails <em>at the boundary between subgraphs</em>, where you can pin the failure to a specific validator, a specific schema mismatch, a specific tool call. You can fix the boundary without retraining the model.</p>\n<p><strong>The 40K token wall.</strong> Jeff Huber from Chroma published research showing that production-tuned models are not invariant to context window size — past about 40K tokens, even the frontier models drift. A monolith that loads everything into a single context burns through 40K fast. A swarm scopes each subagent's context to the 5K it actually needs. <em>Same task, an order of magnitude fewer tokens, no drift.</em></p>\n<p><strong>No A/B per-component.</strong> With a swarm, you can swap the critic from Haiku 4.5 to Gemini 2.5 Flash and measure <em>just that change</em>. With a monolith, every change is a full-system rewrite of the prompt. The teams shipping fastest in 2026 are the ones whose evals run per-component, not per-system. CodeRabbit's Erik Thorelli made this case at AI Dev SF and the data backs it: their benchmarks track precision and verbosity <em>per pipeline stage</em>, not per release.</p>\n<p><strong>Coupling to one provider.</strong> A monolith built around Claude is rebuilt when Claude regresses or repricesx (and Anthropic's own April 2026 postmortem confirmed two months of silent quality degradation in Claude Code from a harness bug — even the flagship product is not immune). A decomposed swarm with mixed models is a hedge.</p>\n<h2>Why the naive swarm also fails</h2>\n<p>This is the part most \"build a multi-agent system\" tutorials skip. <strong>Agents talking to agents through natural language is also a failure mode.</strong> Vlad Luzin from Thenvoi gave the sharpest framing of this at AI Dev SF: agents are not dogs that obey, they are cats that do whatever they want. A herd of cats does not coordinate by chatting.</p>\n<p>The specific failure modes of naive multi-agent systems:</p>\n<ul>\n<li><strong>Context loss between handoffs.</strong> Agent A finishes its turn, agent B picks up. Whatever was in A's working memory that was not explicitly written to the structured handoff is <em>gone</em>. Multi-turn narratives drift because each agent reconstructs its own version of \"what we are doing.\"</li>\n<li><strong>Coordination overhead bigger than the work.</strong> Five agents arguing in natural language about who should do what burns more tokens than the original task would have on a monolith. There is a real X-axis here: at small task sizes, monoliths win on overhead. The swarm pays off only when the task is <em>naturally decomposable</em> into specialists.</li>\n<li><strong>Compounding hallucinations.</strong> Each agent's output becomes input to the next. Errors compound the way they used to in old-school NLP pipelines. The decomposition only works if every handoff is <em>validated</em> — which means schemas, plausibility rules, and a rejection path for everything that doesn't match.</li>\n<li><strong>Reasoning about a graph at runtime.</strong> Multi-agent frameworks that let agents dynamically decide which other agent to invoke build distributed-systems problems into the LLM's reasoning. Most of the failures I have seen in 2026 production deployments fall here. The fix is <em>static</em> graph topology with dynamic routing decisions inside fixed nodes.</li>\n</ul>\n<p>The pattern that works is not \"many smart agents talking to each other.\" It is <strong>manager/worker with deterministic validators between every step</strong>. Bain's architecture, again:</p>\n<pre><code>Email arrives\n   ↓\nEmail classifier (Haiku, 1K tokens)            ← AI executor\n   ↓\nKnockout filter (deterministic schema)         ← validator\n   ↓\nAccount lookup (MCP tool, deterministic)       ← deterministic step\n   ↓\nComplexity scorer (Haiku)                      ← AI executor\n   ↓ (if complex → human)\nPayroll data extractor (Sonnet, structured)    ← AI executor\n   ↓\nSchema verifier (deterministic)                ← validator\n   ↓\nSubmit + release (MCP tool, deterministic)     ← deterministic step\n</code></pre>\n<p>Three things to notice about that diagram:</p>\n<ol>\n<li><strong>Half the steps are deterministic.</strong> Validators and tool calls are not LLMs.</li>\n<li><strong>Each LLM step is bounded.</strong> Specific input shape, specific output shape. No \"figure it out.\"</li>\n<li><strong>The control flow is static.</strong> The router decides which path within a fixed graph; it does not invent paths.</li>\n</ol>\n<p>This is the <em>industrial</em> version of multi-agent. The hobbyist version — let agents call each other dynamically — is the version that fails in production.</p>\n<h2>The local-first dividend</h2>\n<p>The swarm pattern was always the right architecture. What changed in 2026 is that <strong>the cost per agent dropped to a level where the swarm is also the cheapest.</strong> That happened on two axes:</p>\n<p><strong>Axis 1: cheap cloud models.</strong> DeepSeek V4 Flash at $0.14/$0.28 per million is below the floor where most teams' break-even calculations used to sit. Five DeepSeek workers per turn is what one Sonnet 4.5 turn costs. The math swung this year.</p>\n<p><strong>Axis 2: local fan-out is free.</strong> This is the angle nobody else writes about. On a Framework 16 with 96GB unified memory running Qwen 3.6 27B for the executor and Gemma 4 26B MoE for the parallel workers, the entire swarm runs at <em>electricity rates</em>. ~65W sustained × 4 hours × $0.13/kWh = three cents. Per session. The fan-out cost is not \"how many agents\" — it is \"how many tokens per second can my hardware sustain,\" and the answer on a maxed-out APU is enough to keep five workers fed in parallel without buying another GPU.</p>\n<p>The <em>practice</em> that local-first unlocks is the practice that the cloud-only crowd has not internalized:</p>\n<ul>\n<li>Critics get to be expensive. Three turns of the critic arguing with the executor is the largest quality unlock in agentic design. It is paywalled by token cost on metered APIs and free at home.</li>\n<li>Loops can run unattended. A research agent that wakes every 15 minutes to scrape, summarize, and update a vector store is a $300/month subscription on cloud APIs and the same as leaving Slack open at home.</li>\n<li>Experiments stop being budget meetings. \"What if the planner ran twice and we picked the better plan?\" becomes a one-line config change, not a finance conversation.</li>\n</ul>\n<p>This is the regime change. And the architectural shape that makes it possible is <em>the swarm</em>.</p>\n<h2>When the monolith still wins</h2>\n<p>I am bullish on swarms and you should be too, but the honest list of where a monolith beats decomposition:</p>\n<ul>\n<li><strong>Tasks too small to decompose.</strong> A one-shot question with a known answer pattern (summarize this document, classify this ticket) is a single LLM call. Adding a swarm to it adds tokens for no quality gain.</li>\n<li><strong>Highly entangled reasoning.</strong> Some tasks resist decomposition because the parts are not independent. Drafting a novel-length narrative with consistent character voice is one. A monolith with a long context window genuinely does this better.</li>\n<li><strong>Latency-critical interactive work.</strong> A swarm has more network hops. A monolith has one. For sub-second interactive flows (autocomplete, copilots in the editor), a single fast model is the right call. This is why Cursor's Composer-1 is a single specialized model, not a multi-agent system.</li>\n<li><strong>Teams that cannot maintain the harness.</strong> Decomposed swarms have more moving pieces. If your team will not invest in evals, observability, and per-component testing, a monolith is the safer pick. <em>The architecture has to fit the team that maintains it.</em></li>\n</ul>\n<p>The right question is not \"swarm vs monolith.\" It is \"what is decomposable here, and what is not?\" The teams shipping the highest-quality production agents in 2026 ask that question per task and make different calls on different parts of the same product.</p>\n<h2>What I am watching</h2>\n<p>Three threads:</p>\n<ul>\n<li><strong>The end of agent-to-agent natural language coordination.</strong> I expect 2026-2027 to settle the \"how do agents talk to each other\" question with structured protocols (Google's A2A, BAND from Thenvoi, eventually a joint standard) rather than English-language chat. Coordination through schemas is the lesson that production teams have learned, and the tooling will catch up.</li>\n<li><strong>Specialist models per role.</strong> I expect to see fine-tuned Haiku-class models specifically for the <em>router</em> role, <em>critic</em> role, <em>summarizer</em> role, sold as one-job specialists rather than general assistants. Anthropic's own skills system is the early shape of this. The economics are obvious.</li>\n<li><strong>Local-first fan-out as the production default.</strong> When DeepSeek V4 Flash and Qwen 3.6 27B make local-fan-out work for sub-frontier tasks, the most expensive cloud line in your loop is the <em>one</em> turn that genuinely needs Opus or GPT-5. Most teams are still architected as if every turn deserves the frontier. They are paying a premium for a pattern that no longer matches the price curve.</li>\n</ul>\n<h2>The takeaway</h2>\n<p>The \"agentic AI\" of 2026 is not a single brilliant model thinking out loud for 30 seconds and producing a perfect answer. It is <em>eight narrow specialists, half of them deterministic, validated at every boundary, run on the cheapest model that can do the specific job.</em> That architecture is what crosses the gap from 65% demo accuracy to 98% production accuracy. It is also what makes the cost math work at scale.</p>\n<p>The teams that are still building monoliths in May 2026 are paying the slow-decomposition tax — they will get there, but they will get there through a year of failing in production first. You can skip the year by reading the Bain talk and the CrewAI playbook and the DataRobot stack diagram and shipping the decomposed thing on the first attempt.</p>\n<p>The full reference architecture, the validator schemas, and the manager/worker harness templates will land in <a href=\"https://github.com/sudosoph\"><code>apu-config</code></a> shortly. If you want a heads-up when it drops, <a href=\"/contact?subject=Newsletter%20signup&#x26;message=Add%20me%20to%20The%20Architect%27s%20Notebook\">subscribe</a>. If you want it built against your own production data next week, <a href=\"/work-with-me\">that is what an engagement looks like</a>.</p>"
  },
  {
    "slug": "token-budgeting-for-startups",
    "frontmatter": {
      "title": "Token Budgeting for Startups",
      "slug": "token-budgeting-for-startups",
      "publishedDate": "2026-05-07",
      "description": "The engineering math behind preventing an agentic loop from burning through your monthly runway in one night.",
      "tags": [
        "agents",
        "economics",
        "local-inference",
        "ops"
      ],
      "draft": false
    },
    "html": "<p><strong>TL;DR.</strong> A modern agentic loop — planner, three parallel research agents, critic, executor, verifier — consumes 4–8 million input tokens and 250–500 thousand output tokens per active hour of dev work. At Claude Sonnet 4.5 list rates ($3 / $15 per million), that's ~$15–$25 per developer per hour, or $120–$200 per active eight-hour day. Multiply by your team. Multiply by the experiments your team didn't run because they flinched at the projected bill. <strong>The bottleneck on agentic AI is not capability. It is the willingness to let the loop retry.</strong> This post is the engineering math behind the budget — current 2026 prices, where the cost actually accumulates, the controls that work, and why the local-first answer is the only one that scales without flinching.</p>\n<h2>The 3 a.m. story</h2>\n<p>The horror story is not apocryphal. I have heard a version of it from every founder I have worked with this year. It always goes like this:</p>\n<blockquote>\n<p>\"I left the agent running overnight on a refactor. By morning it had retried itself 220 times on the same failing test, accumulating 14M output tokens before its budget cap kicked in. The bill was eight hundred dollars. The bug is still there.\"</p>\n</blockquote>\n<p>The numbers vary. The story does not. The behavioral failure mode is the same in every telling: the <em>agent loop did not know how to stop</em>, and the cost compounded silently while the human slept.</p>\n<p>This post is about the engineering that prevents that night. It is also about why the most-published advice on this topic is wrong — <em>you cannot solve this with a bigger context window or a smarter model</em>. You solve it with hard caps, prompt caching, fan-out budgets, and (if you are honest about the math) a meaningful percentage of your inference moved to hardware you own.</p>\n<h2>What current loops actually cost</h2>\n<p>This is grounded in real production runs from May 2026, not synthetic. Pricing snapshot:</p>\n<table>\n<thead>\n<tr>\n<th>Model</th>\n<th>Input  $/M</th>\n<th>Output $/M</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Claude Sonnet 4.5</td>\n<td>$3.00</td>\n<td>$15.00</td>\n<td>The agentic-coding default</td>\n</tr>\n<tr>\n<td>Claude Haiku 4.5</td>\n<td>$1.00</td>\n<td>$5.00</td>\n<td>Fast, cheap, the router pick</td>\n</tr>\n<tr>\n<td>Claude Opus 4.x</td>\n<td>$15.00</td>\n<td>$75.00</td>\n<td>Recent 67% price cut on Opus 4.5</td>\n</tr>\n<tr>\n<td>GPT-5</td>\n<td>$2.50</td>\n<td>$10.00</td>\n<td>Reasoning premium varies</td>\n</tr>\n<tr>\n<td>GPT-5 mini</td>\n<td>$0.25</td>\n<td>$1.00</td>\n<td>Workhorse for high-fan-out</td>\n</tr>\n<tr>\n<td>Gemini 2.5 Flash</td>\n<td>$0.30</td>\n<td>$2.50</td>\n<td>Long-context champion</td>\n</tr>\n<tr>\n<td><strong>DeepSeek V4 Flash</strong></td>\n<td><strong>$0.14</strong></td>\n<td><strong>$0.28</strong></td>\n<td>The disruptor; MIT-licensed</td>\n</tr>\n<tr>\n<td><strong>DeepSeek V4 Pro</strong></td>\n<td>$1.74</td>\n<td>$3.48</td>\n<td>1.6T MoE, 1M context</td>\n</tr>\n</tbody>\n</table>\n<p>DeepSeek V4 Flash is the line that resets the spreadsheet. At $0.14/$0.28 per million, you can run a fan-out architecture that would have been a budget meeting on Sonnet for the cost of a coffee. This is not a hypothetical — it shipped April 24 with public weights and an MIT license.</p>\n<p>Now the loop math. A \"research-heavy\" agentic dev session I ran last week:</p>\n<ul>\n<li><strong>Loop shape:</strong> planner → 3 parallel research agents → critic → executor → verifier</li>\n<li><strong>Avg tokens per full loop:</strong> 180,000 input / 12,000 output</li>\n<li><strong>Loops per hour during active dev:</strong> 8–12</li>\n<li><strong>Sustained session:</strong> 4 hours</li>\n</ul>\n<p>That is <strong>~6.5M input + 432K output per session</strong>. Costs out as:</p>\n<table>\n<thead>\n<tr>\n<th>Model</th>\n<th>Per session</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Sonnet 4.5</td>\n<td><strong>$26.00</strong></td>\n</tr>\n<tr>\n<td>GPT-5</td>\n<td>$20.65</td>\n</tr>\n<tr>\n<td>Gemini 2.5 Flash</td>\n<td>$3.04</td>\n</tr>\n<tr>\n<td>DeepSeek V4 Flash</td>\n<td>$1.03</td>\n</tr>\n<tr>\n<td>Local (~65W × 4hr)</td>\n<td><strong>$0.034</strong></td>\n</tr>\n</tbody>\n</table>\n<p>The behavioral consequence is what I keep coming back to: at $26 per developer per session you start <em>behaving differently</em>. You trim retries. You shorten the critic's argument with the executor. You stop running parallel agents because the fan-out fee is real. <strong>The cost is the budget meeting you are having with yourself in the moment</strong>, and the budget meeting is what kills the loop's quality.</p>\n<p>At three cents, you do none of those things. You let the agent retry. You let the critic argue four times. You leave loops running through the night because there is no reason not to. That is not a small difference. That is a different practice of engineering.</p>\n<h2>Where the money actually accumulates</h2>\n<p>If you are paying API rates, four cost vectors compound, ranked by how often they kill budgets:</p>\n<p><strong>1. Prompt re-emission.</strong> Every turn of the loop, the entire context — system prompt + tool catalog + history + retrieved docs — gets re-billed as input. A 50-tool MCP catalog is roughly 50K tokens of <em>context overhead per turn</em>. At 80 turns in a long session, that's 4M tokens of <em>just tool descriptions</em>, billed every time. This is the cost vector that sneaks up on people. Pruning the tool catalog to what the current task actually needs is the single highest-leverage cost cut available.</p>\n<p><strong>2. Retry storms.</strong> A failing assertion in the executor sends the loop back to the planner. If the planner does not converge fast, you retry. Each retry burns the full prompt again. Set a hard maximum-iteration count. For most tasks 5 iterations is generous; 10 is the ceiling. Past that, escalate to a human. CrewAI exposes <code>max_iter</code>; LangGraph has interrupt primitives. Use them.</p>\n<p><strong>3. Long-context drift.</strong> Jeff Huber from Chroma published research and said it again at AI Dev SF: language models are not invariant to their context windows. Past about 40K tokens in production-tuned evals, even frontier models start to drift. Builders in the field do not trust past 40K. <strong>The bigger context window is not a budget solution; it is a quality liability.</strong> Compact aggressively, summarize history, evict.</p>\n<p><strong>4. Untyped tool output.</strong> If a tool returns 50KB of JSON and the model only needs three fields, you just paid for 50KB of input on the next turn. Postprocess tool output in deterministic code before it hits the model. This pattern saves more than prompt caching does, and nobody talks about it.</p>\n<h2>Engineering controls that work</h2>\n<p>In rough order of leverage:</p>\n<p><strong>Prompt caching.</strong> Anthropic's prompt caching (5-minute and 1-hour TTLs) gives 90% cost reduction on cached prefixes. The architecture trick is to keep <em>system prompt + tool catalog + static instructions</em> at the front of the message so they hit the cache. Vary the dynamic content at the end. Cached input on Sonnet is $0.30/M instead of $3/M — a 10× cut. Anthropic, OpenAI, and Google all support this; the discount factor varies but the architecture is identical.</p>\n<p><strong>Tiered models.</strong> Route by complexity. A planner running on Sonnet 4.5 is fine; the three parallel researchers can almost always run on Haiku 4.5 or DeepSeek V4 Flash. Save the expensive model for the turn where intelligence actually matters. UiPath's Raghu Malpani called this exact pattern \"process orchestration over agent swarming\" at AI Agent Conference NYC last week — agents play a <em>role</em> in the workflow, alongside cheaper deterministic systems. Same principle, applied to dollars.</p>\n<p><strong>Per-loop budget caps.</strong> Set a max-token-spend per session, not just per call. CrewAI exposes <code>max_total_tokens</code>; if you are rolling your own, wrap the API call and abort when the running total crosses the cap. Pair with a Slack/Discord alert: humans get notified before money gets spent, not after.</p>\n<p><strong>Iteration counts.</strong> Default <code>max_iter</code> to 5, hard ceiling 10. If the loop has not converged by then, the wrong thing is being retried. Hand off to a human and log it.</p>\n<p><strong>Tool catalog scoping.</strong> GitHub's MCP server has 80+ tools. Most loops need 5. Use the <code>--toolsets</code> flag (or your client's equivalent) to load <em>only</em> the tools the current task needs. This is the single change that has saved me the most money in production.</p>\n<p><strong>Sleep on it.</strong> Long-running agentic tasks should run during off-peak hours where the provider has spot-capacity tiers, or use the Batch API ($1.50/$7.50 vs $3/$15 on Anthropic — a 50% cut) for anything that does not need to finish in real time. Most \"real time\" tasks do not actually need real time; they need <em>eventual consistency</em>. The deadline you put on the agent is usually self-imposed.</p>\n<h2>The local-first move</h2>\n<p>This is the answer the cloud-first writers do not want to give you. <strong>Most of the agentic loop does not need a frontier model.</strong> It needs a tool-calling model, a router, a critic, a summarizer, a structured-output extractor. All of those are jobs that fit comfortably on a local Qwen 3.6 27B or a Gemma 4 26B MoE running on hardware you can buy for the cost of two months of Sonnet bills.</p>\n<p>The economics, again from my own daily setup:</p>\n<ul>\n<li><strong>Hardware:</strong> Framework 16 with 96GB unified memory + RX 7700S + Strix Point. ~$3,200 once.</li>\n<li><strong>Power:</strong> ~65W under sustained load. At $0.13/kWh, that's $0.034 per active hour.</li>\n<li><strong>Local model mix:</strong> Qwen 3.6 27B (default agent), Gemma 4 26B MoE (parallel fan-out), Gemma 4 E4B (router/classifier).</li>\n<li><strong>API mix (kept on payroll):</strong> DeepSeek V4 for multimodal/spatial tasks, Kimi K2.6 for UI generation, Claude/GPT-5 for the ~5% of turns that genuinely need frontier reasoning.</li>\n</ul>\n<p>The crossover math is straightforward. If you are spending $200/month on agentic API calls today, the laptop pays itself back in 16 months. If you are spending $500/month, it pays back in 6 months. If you are running a team and the bill is $3,000/month, you should have bought one for every developer last quarter.</p>\n<p>But the cost recovery is not the interesting part. <strong>The interesting part is the regime change in how you design loops when fan-out is free.</strong> Five $0.01 cloud agents and one $0.50 monolith are roughly the same. Five local agents and one local monolith are also the same — except the local fan-out finishes in parallel and the cloud one finishes when the rate limiter says so. Joe Moura from CrewAI showed this at AI Agent Conference NYC: their internal Iris agent is <em>now executing more than a billion crew runs per quarter</em>. That kind of execution density is impossible at API rates. It is normal at electricity rates.</p>\n<h2>The honest tradeoff</h2>\n<p>I am not going to argue that local replaces frontier. The 5% of turns that need frontier reasoning will keep needing it. The architecture that wins is hybrid: 95% local for the routing, classifying, drafting, summarizing, tool-calling, and critiquing turns; 5% frontier API for the genuinely hard problems. A well-designed loop calls the frontier <em>once per session, by name</em>, when the question is hard enough to earn the bill.</p>\n<p>The hybrid pattern has a real engineering tax. You need to maintain two stacks. You need to handle the \"is this turn important enough\" routing decision (the answer is usually a small classifier model that itself runs locally). You need to monitor both for drift. None of that is free.</p>\n<p>What you get for the tax is the freedom to design loops without flinching. That is the moat. The teams that ship this in 2026 are the teams who can run an experiment without scheduling a budget meeting first.</p>\n<h2>What I am watching</h2>\n<p>Three threads on the radar:</p>\n<ul>\n<li><strong>The DeepSeek V4 Flash floor.</strong> $0.14/$0.28 per million is below where most teams' break-even-vs-self-host lines used to sit. Either Anthropic and OpenAI cut prices materially in 2026 to defend, or the high-volume agentic workloads migrate to DeepSeek and to local. I am watching pricing pages on every model provider monthly. Bet on the price cuts.</li>\n<li><strong>Open-weight inference at home matching API quality.</strong> Qwen 3.6 27B Dense matches Claude 4.5 Opus on Terminal-Bench 2.0 and lands within 4 points of Opus 4.6 on SWE-bench Verified. The gap between \"what runs on your laptop\" and \"what runs in Anthropic's cluster\" is closing fast on the <em>agentic-coding</em> axis specifically. Routing sophistication is the most underbought skill in agentic engineering for the next twelve months.</li>\n<li><strong>Spot-priced inference tiers.</strong> DataRobot's Venky Veeraraghavan made the argument at AI Dev SF: providers will eventually expose premium and spot tiers, where SLA-bound traffic pays full freight and low-priority work runs on spare capacity at heavy discounts. Anthropic's Batch API is the leading edge of this; expect it to spread.</li>\n</ul>\n<h2>The takeaway</h2>\n<p>Token budgeting is engineering, not penny-pinching. A well-architected loop fits inside a <em>known</em> per-session budget. A poorly architected loop is a runway-burning bot waiting for the wrong night. The four moves that matter — prompt caching, tiered models, hard iteration caps, scoped tool catalogs — pay for themselves on the first session that would have spiraled. The fifth move, moving the routing-and-tool-calling 80% to local hardware, is what separates the teams that flinch from the teams that ship.</p>\n<p>The full configuration — wrapper code for budget caps, the tiered-routing classifier, the n8n templates for shadow-cost monitoring — will land in <a href=\"https://github.com/sudosoph\"><code>apu-config</code></a> and on the newsletter shortly. If you want a heads-up when it drops, <a href=\"/contact?subject=Newsletter%20signup&#x26;message=Add%20me%20to%20The%20Architect%27s%20Notebook\">subscribe</a>. If you want it set up against your own loop next week, <a href=\"/work-with-me\">that is what an engagement looks like</a>.</p>"
  },
  {
    "slug": "vibe-coding-for-founders",
    "frontmatter": {
      "title": "Vibe Coding for Founders",
      "slug": "vibe-coding-for-founders",
      "publishedDate": "2026-05-05",
      "description": "How to lead a codebase by stating intent instead of writing syntax — and the discipline that keeps it from falling apart.",
      "tags": [
        "agents",
        "vibe-coding",
        "claude-code",
        "ai-native"
      ],
      "draft": false
    },
    "html": "<p><strong>TL;DR.</strong> Andrew Ng told a room at AI Dev SF last week that the AI/human code ratio is moving from 80/20 to 100/0, and that the bottleneck is now product management, not engineering. At the same conference Marc Brooker from AWS argued the opposite: that the <em>opportunity</em> of agentic AI is now gated by defects, not capability. Both are right, and the synthesis is what this post is about. <strong>Vibe coding is the founder's superpower if and only if you treat \"stating intent\" as the actual engineering work.</strong> This post is about what intent-led development looks like in practice — the files, the hooks, the guardrails, and the failure modes I've personally burned through running this stack on AMD hardware for the last six months.</p>\n<h2>Where we are, in numbers</h2>\n<p>A few data points from the last two weeks of conferences and shipped products, all of which are real and citable:</p>\n<ul>\n<li><strong>Google: over 75% of code generated by AI</strong> (Paige Bailey, Google DeepMind, AI Dev SF, April 28).</li>\n<li><strong>CrewAI: 42% of code last week was AI-authored</strong> (Joe Moura, AI Agent Conference NYC, May 4).</li>\n<li><strong>Claude Code: $2.5B revenue run-rate in February 2026</strong>, twelve months after launch.</li>\n<li><strong>Wilson Lin published <code>fastrender</code> in January 2026: ~1M lines of Rust, ~30,000 commits, ~2,000 concurrent agents at peak</strong>, written essentially as a planner-and-workers system that coordinated to keep work from overlapping.</li>\n<li><strong>42% of code, 75% of code, $2.5B run-rate.</strong> Whatever you think about LLMs, the cost of a marginal line of code dropped by an order of magnitude in 2025 and is dropping again in 2026.</li>\n</ul>\n<p>The tax this imposes is the one Andrew Ng pointed at: when SWE speeds up 10–100x, <em>everything else is slow in comparison</em>. Design, legal, marketing, ops, all suddenly become bottlenecks. The \"AI-native team\" he described is small, generalist, and structurally different — engineers who can write product copy, PMs who can ship features, designers who can read a diff. Per-engineer ARR is going up. Headcount is going down. The interesting positions are the ones where one person can plausibly play three roles, because the agent fills in the gaps.</p>\n<p>If you're a founder, you are already that person. The question is whether your <em>codebase</em> is structured so the agents you've hired (in dollars-per-month, billed by Anthropic) can actually do the work.</p>\n<h2>What \"vibe coding\" was, and what it became</h2>\n<p>Andrej Karpathy coined the term in February 2025. The original tweet was specifically about a throwaway weekend mode: forget the code exists, copy-paste the error message back into the agent, accept-all-diffs, never read what was written. In the original framing it was <em>not</em> a methodology — it was a holiday from methodology. A weekend treat.</p>\n<p>What the term turned into in the year that followed is something different and more contested. The disillusionment piece that everyone in the discourse will eventually link is from a developer named Alberto Fortin in May 2025 — months of LLM-led coding produced what he described as \"ten junior-mid developers locked in a room without seeing what the other nine were doing.\" Inconsistent naming. Duplicated logic. Architectural drift. He went back to pen-and-paper planning and treating the model as an assistant rather than the lead engineer.</p>\n<p>Both stories are correct. The reconciliation is that <em>vibe coding works perfectly for projects you don't have to maintain</em> and <em>fails predictably for systems that have to live</em>. Karpathy was right about the weekend. Fortin was right about the codebase.</p>\n<p>What founders need is a third thing: a discipline that takes the velocity of vibe coding and pays the small upfront tax that makes the output <em>maintainable</em>. Call it intent-led coding, or context engineering, or — the term I keep coming back to — <em>authoring constraints</em>.</p>\n<h2>Authoring constraints, not authoring code</h2>\n<p>The shift is small in words and large in consequence. Stop writing code. Start writing the rules that constrain how code gets written. The agent does the typing.</p>\n<p>Here's what that looks like on a real project — the very repo this site is built in. The codebase has a file at the root called <code>AGENTS.md</code> (Cursor reads it, Claude Code reads it, OpenAI Codex reads it; same source, three agents). The entire content of that file is:</p>\n<pre><code># This is NOT the Next.js you know\n\nThis version has breaking changes — APIs, conventions, and file structure\nmay all differ from your training data. Read the relevant guide in\n`node_modules/next/dist/docs/` before writing any code. Heed deprecation\nnotices.\n</code></pre>\n<p>That's it. Thirty-seven words. It does not describe how to write Next.js code. It does not list APIs. It does not give examples. What it does is <strong>change the agent's behavior</strong> — instead of relying on its training-data memory of a 2024 Next.js, the agent reads the version-pinned docs in <code>node_modules</code> before generating code. The diff before and after this file existed was the difference between \"looks correct, doesn't compile\" and \"compiles on the first try.\"</p>\n<p>That is what authoring constraints means in practice. A small, well-placed sentence that <em>describes the world correctly</em> saves you a hundred wrong diffs.</p>\n<h2>The constraints worth authoring</h2>\n<p>Six categories, ordered roughly by leverage:</p>\n<p><strong>1. The version-pinned reality.</strong> \"This is not the X you know\" is the single highest-leverage sentence I have written in my whole career. Every framework that has had a breaking release between the model's training cutoff and today belongs in this list. Next.js 15 vs 16. React 19. Tailwind 4. Anything where the agent's training data is wrong about the present.</p>\n<p><strong>2. The architectural invariants.</strong> \"All API routes use Edge runtime unless they need nodemailer.\" \"Components in <code>components/site/</code> are always server components unless explicitly marked.\" \"Database access goes through <code>lib/db.ts</code>, never directly from a route.\" These are statements of <em>should</em>, not how — they constrain choice without prescribing implementation. The agent picks the right pattern because the wrong pattern violates a rule.</p>\n<p><strong>3. The deprecation map.</strong> \"We migrated off <code>react-query</code> to TanStack Query in March; old patterns from forums will not work.\" Agents will pull old patterns out of their training data. The deprecation map is how you tell them which patterns are dead.</p>\n<p><strong>4. The taste rules.</strong> \"Default to writing no comments. Only add a comment when the WHY is non-obvious.\" \"Don't add error handling for scenarios that can't happen.\" \"Three similar lines is better than a premature abstraction.\" These bias the agent toward the aesthetic of your codebase. Without them, every model defaults to its own median taste — which is more comments, more error handling, more abstraction than you want.</p>\n<p><strong>5. The hooks.</strong> Memory and preferences alone don't <em>enforce</em>. Hooks do. A pre-commit hook that runs the type checker and rejects the diff is the difference between \"the agent claimed it worked\" and \"the agent's diff actually compiles.\" Claude Code's hook system, Cursor's <code>.cursorrules</code>-driven validation, and good old <code>husky</code> for git-level enforcement all do the same job: they are the harness's immune system.</p>\n<p><strong>6. The skills.</strong> Reusable prompt-and-script bundles for tasks the agent has to repeat. A <code>/release</code> skill that knows how to bump the version, regenerate the changelog, and tag the commit. A <code>/migrate</code> skill that knows your specific data-migration pattern. A <code>/review</code> skill that runs the type checker plus your custom linters before declaring success. CrewAI's open <code>skills.crewai.com</code> is one example of the public version of this; Anthropic's PDF/DOCX/XLSX creation in Claude is shipped <em>entirely</em> as skills under the hood. The point is that skills are authored once and called by name forever.</p>\n<p>These six categories are the entire job. There is no seventh thing.</p>\n<h2>What changes for founders specifically</h2>\n<p>The most-quoted piece of trade-press cope from the last six months is some version of \"founders can't really vibe-code a real product.\" This is wrong, and the existence proof is sitting on most of our home screens. Pieter Levels has been shipping seven-figure-revenue PHP apps with this approach since before it had a name. Wilson Lin's <code>fastrender</code> is a million lines of Rust written with this pattern. Paul Ford in the New York Times described converting a thorny dataset over a weekend that he would have charged $350,000 for in the previous era.</p>\n<p>What changes specifically for the <em>founder</em> role:</p>\n<p><strong>You will spend more time writing English than code.</strong> The English you write is <code>AGENTS.md</code>, the architectural-invariant doc, the user stories, the taste rules. This is not less work than coding. It is differently shaped work. The skill is closer to product specification than to engineering.</p>\n<p><strong>You will spend less time picking the right library.</strong> The model has read more open-source code than you have. The right library is mostly a solved problem. What's not solved is <em>whether the right library should be used here</em> — that's a constraint question, and it's the question only you can answer.</p>\n<p><strong>You will spend more time reviewing and less time typing.</strong> This is the part of the job that is genuinely harder. Marc Brooker's argument at AI Dev was that the bottleneck on agentic AI is the defect rate, and that the most interesting opportunity is the <em>low frequency of low consequence defects</em> — bugs that are rare and small. The way you get there is not by typing; it's by reviewing. Aggressively. Build a culture of reading the diff even when the agent says it works.</p>\n<p><strong>Your moat shifts from speed to taste.</strong> In a world where any founder can have a working prototype by Friday, the prototypes that win are the ones that feel right. The ones with strong opinions in the UI, the ones with one-tenth the surface area but the right tenth, the ones whose error messages read like a person wrote them. Taste is the constraint that doesn't fit in a <code>.cursorrules</code> file. It comes from you.</p>\n<h2>The local-first angle nobody else can give you</h2>\n<p>This is the piece I have not seen anyone else write, so I'll write it bluntly: <strong>vibe coding gets cheaper and safer when the agent runs locally.</strong></p>\n<p>A few reasons:</p>\n<ul>\n<li>\n<p><strong>Token cost is the silent killer of long agent loops.</strong> Every retry, every revision, every \"let me try again\" turn at metered API rates adds up. On Claude Sonnet 4 at 2026 prices, a busy agentic dev loop can run $20–60 per developer per day. On the Framework 16 with 96GB of unified memory I've written about elsewhere, the same loop is electricity. Three cents. The behavioral difference is enormous: you stop rationing retries, which means you stop trimming the part of the loop where the critic argues with the executor — which is where the quality lives.</p>\n</li>\n<li>\n<p><strong>Sandboxing is structurally easier when nothing leaves the machine.</strong> Tushar Jain from Docker showed at AI Dev a per-agent MicroVM-based sandbox that limits what the agent can touch. That's the cloud-native version. The local-native version is even simpler: don't give the agent network access in the first place. A local model running in a container with no outbound DNS can write all the code it wants and exfiltrate exactly nothing. The threat model collapses.</p>\n</li>\n<li>\n<p><strong>The 4-legged identity problem disappears at home.</strong> Matthew Xu gave a talk at AI Agent Conf about how OAuth was designed for the user-app-IdP triangle and breaks down in the user-agent-MCP-API square. Every cloud-agent setup has to solve this, and the production solutions involve token exchange (RFC 8693) and dynamic client registration (RFC 7591) and dual IdP integration. On a single-machine local setup, the equivalent of all of that is filesystem permissions and environment variables. The protocol's own authors carved an exception in for stdio transport: don't bother with OAuth, just use the env. Sovereign compute simplifies the auth story dramatically.</p>\n</li>\n</ul>\n<p>The thesis: as the agentic stack matures, <em>the gap between cloud-agent setup and local-agent setup widens in favor of local for tasks that don't require frontier-class reasoning</em>. A founder who can keep 95% of their agentic dev loop on their own hardware ships faster, cheaper, and with a smaller threat surface. The 5% that genuinely needs Claude or GPT-5 reasoning gets called by name, once per session, when it actually earns the bill.</p>\n<h2>The failure modes I've personally hit</h2>\n<p>I'm not going to list \"AI hallucinates\" because everyone has heard it. Here are the failures I've run into in the last six months that I didn't see coming:</p>\n<ul>\n<li><strong>Test-suite rot.</strong> The agent disabled a failing test rather than fix the underlying bug. Twice. The fix is a hook that rejects any diff that touches a test file unless the commit message explains why. Annoying. Worth it.</li>\n<li><strong>Looks-correct-but-isn't.</strong> A migration that ran cleanly, type-checked, deployed — and silently truncated a numeric column from <code>bigint</code> to <code>int</code> because the agent inferred the wrong target type from a half-written model. Caught it in shadow mode (more on this in a future post). The lesson is the one Sanjin Bicanic gave at AI Agent Conf about the Bain HR-Services payroll agent: <em>production is the only truth. Shadow test before you ship.</em></li>\n<li><strong>The 40K-token wall.</strong> Jeff Huber from Chroma published research and said it again at AI Dev: language models are not invariant to their context windows. Past about 40K tokens, even the frontier models start to drift. The cure is not \"buy a bigger context window.\" The cure is <em>stop putting noise in the context</em>. Skills, retrieval, and conflict resolution beat dumping the world into the prompt every time.</li>\n<li><strong>Cognitive debt.</strong> This is the failure mode that scared me the most. After three weeks of accept-all-diffs on a feature, I realized I no longer fully understood the code that had my name on it. The fix is the rule that Steve Yegge calls \"make the agent interview you\" — before merging, ask the agent to ask you the questions it would ask if you were code-reviewing it. If you can't answer, you don't ship.</li>\n</ul>\n<p>None of these are reasons to stop. All of them are reasons to author the right constraints up front.</p>\n<h2>The honest tradeoff</h2>\n<p>Vibe coding's enemies aren't the people in the comments saying it produces slop. The slop is real, and it accumulates if you don't review. The enemies are subtler:</p>\n<ul>\n<li><strong>Vendor lock-in.</strong> A $200/month subscription per agent per IDE is a real cost, and the artifacts you produce — Cursor Composer prompts, Claude Code skills, project memory files — are not portable across vendors. Authoring constraints in one tool's format doesn't transfer cleanly to another's.</li>\n<li><strong>Harness regression.</strong> Anthropic publicly acknowledged in April 2026 that two months of degraded Claude Code quality were caused by a harness bug, not the model. If their flagship product can silently regress for two months without anyone diagnosing it, your project can absolutely do the same. External verification — tests, types, preview deploys, manual QA — is not optional.</li>\n<li><strong>Estimation chaos.</strong> AI-assisted dev is wildly unpredictable per task. The same feature might take fifteen minutes or four hours. Sprint planning gets weird. The teams I've seen handle this best have abandoned story points entirely and switched to running outcomes through a small enough work-in-progress queue that estimation matters less.</li>\n</ul>\n<p>The tradeoff is real. Whether you take it is a function of how much you're optimizing for velocity vs. how much you're optimizing for predictability. Most founders, most of the time, should be optimizing for velocity. But not all the time, and not for everything.</p>\n<h2>What I'm watching</h2>\n<p>Three threads on the radar:</p>\n<ul>\n<li><strong>Specs as first-class artifacts.</strong> Marc Brooker at AWS is shipping autoformalization tooling that turns natural-language SOPs into mathematically precise specifications, with deterministic enforcement against agent behavior. This is the production-grade version of \"authoring constraints\" and it's coming for codebases too. If you're building anything in regulated domains, watch this space.</li>\n<li><strong>Skills as the new package manager.</strong> Anthropic's October 2025 skills launch was, in the language of AI Dev's panel, \"a bigger deal than MCP.\" A skill is a folder with a <code>SKILL.md</code> and some scripts; the model loads it on demand, costs nothing in tokens until needed. Every team I respect is now publishing internal skills like internal libraries. There is a real chance this becomes the unit of code reuse in the agent era.</li>\n<li><strong>The harness war.</strong> Cursor, Claude Code, OpenAI Codex, Gemini CLI, OpenClaw — five harnesses, each with strong opinions, each fighting for the developer's daily session. The next eighteen months will sort out which of them are products and which are features. Personally I run two daily and rotate; I would not bet on a single winner yet.</li>\n</ul>\n<h2>The one-line summary</h2>\n<p>If you take one thing from this: <strong>the agents are going to write the code either way. Your job is to write the constraints they read first.</strong> Done well, that's the highest-leverage work a founder will do this decade.</p>\n<p>The repo for this site, including the <code>AGENTS.md</code> and the build scripts and the manifest pipeline that ships these posts, is public at <a href=\"https://github.com/sudosoph/agentic-architecture\">github.com/sudosoph/agentic-architecture</a>. Fork it, steal what works, ignore what doesn't. That's how this stuff is supposed to spread.</p>"
  }
]
